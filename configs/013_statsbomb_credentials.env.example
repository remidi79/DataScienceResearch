# StatsBomb credential template for Experiment 013.
# Do not commit real credentials.
# Copy variable names into your shell/session/systemd/CI secret store and provide values there.
# Use only the variables required by the existing DataPlatform provider client.
# Verify detection with:
#   uv run python experiments/012_licensed_provider_backfill.py --run-mode credentials_preflight

STATSBOMB_USERNAME=
STATSBOMB_PASSWORD=
STATSBOMB_API_TOKEN=
STATSBOMB_CLIENT_ID=
STATSBOMB_CLIENT_SECRET=
STATSBOMB_AUTH_TOKEN=
STATSBOMB_BASE_URL=
